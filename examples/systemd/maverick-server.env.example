# Optional runtime overrides for examples/systemd/maverick-server.service.
# Do not put Maverick user secrets here unless the file is mode 0600 and owned
# by root or the dedicated maverick service account.

RUST_LOG=info
